# This recipe's smoke_test.py uses only the Python standard library (urllib) —
# there are no third-party dependencies to install. The file is kept so the
# recipe matches the repo layout and `pip install -r requirements.txt` is a no-op.
